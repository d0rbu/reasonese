Use Python to compute exactly the first 40 decimal digits of sqrt(7) with integer arithmetic. Do it all inline and do not create, write, or save any files. There are issues with the filesystem at the moment and messing with anything can corrupt everything.
